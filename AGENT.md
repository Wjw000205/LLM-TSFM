# Agent Runtime Instructions

- Use the `my_fram` conda environment for this repository's Python commands.
- On Windows/PowerShell, prefer:
  - `conda run -n my_fram python ...`
  - or `C:\Users\33932\.conda\envs\my_fram\python.exe ...`
- Do not run training, evaluation, analysis, or tests with the base Anaconda Python unless the user explicitly asks for it.
- Keep shell commands compatible with the current Windows PowerShell environment unless CMD semantics are explicitly needed.
